{
  "schemaVersion": "norman.research.crmx_compatibility.v1",
  "repository": "JDMD23/NormanAI-CRMx",
  "sourceOfTruth": "NormanAI-CRMx (SQLite). Research is discovery/qualify only — never scores Fit, never writes Notion as SoR.",
  "verifiedPublicIntake": {
    "module": "norman.tools.ingest_csv",
    "invocation": "uv run python -m norman.tools.ingest_csv <csv> <db> --added-from <label> --evidence <path.json>",
    "evidence": "CRMx fail-closed evidence ingest (schema norman.research.crmx_evidence.v1). Research promote always passes --evidence; missing sidecar fails closed. See NormanAI-CRMx docs/research-evidence-handoff.md.",
    "csvContract": "Crunchbase-export column names (Organization Name, Website, LinkedIn, …). Research remaps its internal candidate fields into this shape before promote.",
    "richerIntake": "Research always writes a versioned evidence JSON sidecar (nyc_evidence, source_urls, keyword_hits, signal_notes) beside the CSV and passes it to ingest_csv via --evidence. Research will not invent Notion writes or Fit scores to preserve evidence."
  },
  "evidenceSidecarSchemaVersion": "norman.research.crmx_evidence.v1",
  "fundingHandoff": {
    "status": "adapted_via_ingest_csv",
    "requestSchema": "norman.research.funding_handoff.v1",
    "adapter": "Research maps typed funding events → CRMx CSV + norman.research.crmx_evidence.v1 sidecar, then calls norman.tools.ingest_csv --evidence (same public surface as ordinary promote).",
    "typedCli": "none verified — CRMx returned 404 to this agent's token and no public funding-handoff CLI was found. Do not invent Notion/Fit writers from Research.",
    "resultSynthesis": "On successful ingest_csv, Research synthesizes ledger-compatible norman.crm_core.funding_handoff_result.v1 with state=created and pageId=crmx:ingest:<eventKey>. Follow-up for CRMx: publish a typed funding CLI that returns durable per-event terminal states (created/queued_existing/duplicate_event/…) so Research can stop synthesizing.",
    "legacyFlag": "--handoff-legacy-crm-core",
    "env": ["NORMAN_CRMX_PATH", "NORMAN_CRMX_DB"]
  },
  "legacyShim": {
    "flag": "--promote-legacy-crm-core",
    "fundingFlag": "--handoff-legacy-crm-core",
    "configTarget": "legacy_crm_core",
    "cli": "scripts/crm_intake.py",
    "fundingCli": "scripts/crm_funding_handoff.py"
  }
}
